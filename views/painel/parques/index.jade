.col-lg-12(ng-init='load()')
  .box
    .box-header(data-original-title='')
      h2
        i.fa.fa-female
        span.break
        | Parques
      .box-icon
        a.btn-plus(href='#/parques/adicionar')
          i.fa.fa-plus

    .box-content
        .block
          div(class='alert alert-{{ status.type }}', ng-show='status')
              button(type='button', class='close', data-dismiss='alert') &times;
              | {{ status.message}}

          div(class='alert alert-warning', ng-show='parques.length == 0')
              | Você não possui nenhum parque cadastrado, cadastre um &nbsp;
              a(href='#/parques/adicionar') aqui

          table.table.table-striped.table-bordered.bootstrap-datatable.datatable(ng-show='parques.length')
              thead
                  tr
                      th Título
                      th Descrição
                      th

              tbody(ng-show='parques.length')
                  tr(ng-repeat='parque in parques | pagination: curPage * pageSize | limitTo: pageSize')
                      td {{ parque.titulo }}
                      td {{ parque.descricao }}
                      td
                          a(href='#/parques/editar/{{ parque._id }}', class='btn btn-mini btn-success')
                              span.fa.fa-edit &nbsp;
                          button(class='btn btn-mini btn-danger', ng-click='delete(parque._id)')
                              span.fa.fa-trash-o &nbsp;

      .block.text-center(ng-show='parques.length')
        ul.pagination.pagination-centered.inline.unstyled
          li
            a.btn.btn-link(ng-disabled='curPage === 0', ng-click='curPage = curPage - 1')
              | Voltar
          li
            span
              | Página {{ curPage + 1 }} de {{ paginas() }}
          li
            a.btn.btn-link(ng-disabled='curPage >= parques.length / pageSize - 1', ng-click='curPage = curPage + 1')
              | Avançar