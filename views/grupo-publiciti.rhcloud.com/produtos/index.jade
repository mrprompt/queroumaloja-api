.col-lg-12(ng-init='load()')
  .box
    .box-header(data-original-title='')
      h2
        i.fa.fa-book
        span.break
        | produtos
      .box-icon
        a.btn-plus(href='#/produtos/adicionar')
          i.fa.fa-plus

    .box-content
        div(class='alert alert-{{ status.type }}', ng-show='status')
            button(type='button', class='close', data-dismiss='alert') &times;
            | {{ status.message}}

        div(class='alert alert-warning', ng-show='produtos.length == 0')
            | Você não possui nenhum produto cadastrado, cadastre um &nbsp;
            a(href='#/produtos/adicionar') aqui

        table.table.table-striped.table-bordered.bootstrap-datatable.datatable(ng-show='produtos.length')
          thead
            tr
                td Título
                td Código
                td Tipo
                td Categoria
                td.col-md-1

          tbody(ng-show='produtos.length')
            tr(ng-repeat='produto in produtos | pagination: curPage * pageSize | limitTo: pageSize')
              td {{ produto.titulo }}
              td {{ produto.codigo }}
              td {{ produto.tipo }}
              td {{ produto.categoria }}
              td
                a(href='#/produtos/editar/{{ produto._id }}', class='btn btn-mini btn-success')
                  span.fa.fa-edit
                button(class='btn btn-mini btn-danger', ng-click='delete(produto._id)')
                  span.fa.fa-trash-o

          tfoot
            tr
              td(colspan='3', class='text-left')
                b {{ produtos.length }}
                | &nbsp; produtos cadastrados
              td(colspan='2', class='text-right')
                a(href='#/produtos/adicionar', class='btn btn-primary btn-mini')
                  span.fa.fa-plus &nbsp;

      .block.text-center(ng-show='produtos.length')
        ul.pagination.pagination-centered.inline.unstyled
          li
            a.btn.btn-link(ng-disabled='curPage === 0', ng-click='curPage = curPage - 1')
              | Voltar
          li
            span
              | Página {{ curPage + 1 }} de {{ paginas() }}
          li
            a.btn.btn-link(ng-disabled='curPage >= autacoes.length / pageSize - 1', ng-click='curPage = curPage + 1')
              | Avançar