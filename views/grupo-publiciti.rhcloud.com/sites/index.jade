.col-lg-12(ng-init='load()')
  .box
    .box-header(data-original-title='')
      h2
        i.fa.fa-globe
        span.break
        | Sites
      .box-icon
        a.btn-plus(href='#/sites/adicionar')
          i.fa.fa-plus

    .box-content
        .block
            div(class='alert alert-{{ status.type }}', ng-show='status')
                button(type='button', class='close', data-dismiss='alert') &times;
                | {{ status.message}}

          div(class='alert alert-warning', ng-show='sites.length == 0')
              | Você não possui nenhum site cadastrado, cadastre um &nbsp;
              a(href='#/sites/adicionar') aqui

            table.table.table-striped.table-bordered.bootstrap-datatable.datatable(ng-show='sites.length')
                thead
                    tr
                        td.col-md-4 Nome
                        td.col-md-3 E-mail
                        td.col-md-2 Domínio
                        td.col-md-2 Módulos
                        td.col-md-1

                tbody(ng-show='sites.length')
                    tr(ng-repeat='site in sites | pagination: curPage * pageSize | limitTo: pageSize')
                        td {{ site.nome }}
                        td {{ site.email }}
                        td {{ site.dominio }}
                        td {{ site.modulos }}
                        td
                            a(href='#/sites/editar/{{ site._id }}', class='btn btn-mini btn-success')
                                span.fa.fa-edit &nbsp;
                            button(class='btn btn-mini btn-danger', ng-click='delete(site._id)')
                                span.fa.fa-trash-o &nbsp;

      .block.text-center(ng-show='sites.length')
        ul.pagination.pagination-centered.inline.unstyled
          li
            a.btn.btn-link(ng-disabled='curPage === 0', ng-click='curPage = curPage - 1')
              | Voltar
          li
            span
              | Página {{ curPage + 1 }} de {{ paginas() }}
          li
            a.btn.btn-link(ng-disabled='curPage >= sites.length / pageSize - 1', ng-click='curPage = curPage + 1')
              | Avançar

